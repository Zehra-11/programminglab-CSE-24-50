#include <stdio.h>
int main (){
     printf("%d",16%10);   // modulo only works on integers , 
                            //for+ve numerator remainder +ve, for -ve numerator remainder is -ve


    return 0;
}